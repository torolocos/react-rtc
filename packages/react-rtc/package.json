{
  "name": "@torolocos/react-rtc",
  "version": "1.0.0",
  "description": "React real-time communication library.",
  "keywords": [
    "webrtc",
    "react"
  ],
  "homepage": "https://github.com/torolocos/react-rtc#readme",
  "license": "MIT",
  "author": "torolocos",
  "files": [
    "dist"
  ],
  "main": "./dist/esm/index.js",
  "types": "./dist/types/index.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/torolocos/react-rtc.git"
  },
  "bugs": {
    "url": "https://github.com/torolocos/react-rtc/issues"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch --preserveWatchOutput",
    "tsc:check": "tsc --pretty --noEmit",
    "lint": "eslint . --ext ts --ext tsx",
    "test": "jest",
    "test:watch": "jest --watch",
    "preversion": "git pull && yarn lint && yarn build",
    "postversion": "yarn publish --version $npm_package_version --access public && git push --tags"
  },
  "peerDependencies": {
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@testing-library/react-hooks": "^8.0.1",
    "@torolocos/eslint-config": "*",
    "@types/jest": "^28.1.3",
    "@types/react": "^18.0.12",
    "jest": "^28.1.3",
    "react-test-renderer": "^18.2.0",
    "ts-jest": "^28.0.7",
    "typescript": "^4.7.3"
  }
}
