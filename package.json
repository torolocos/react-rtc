{
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint --parallel",
    "tsc:check": "turbo run lint --parallel",
    "publish:patch": "yarn workspace @torolocos/react-rtc version --patch",
    "publish:minor": "yarn workspace @torolocos/react-rtc version --minor",
    "publish:major": "yarn workspace @torolocos/react-rtc version --major",
    "publish:prepatch": "yarn workspace @torolocos/react-rtc version --prepatch",
    "publish:preminor": "yarn workspace @torolocos/react-rtc version --preminor",
    "publish:premajor": "yarn workspace @torolocos/react-rtc version --premajor",
    "publish:prerelease": "yarn workspace @torolocos/react-rtc version --prerelease",
    "postinstall": "husky install",
    "changelog": "auto changelog"
  },
  "devDependencies": {
    "@auto-it/all-contributors": "^10.37.4",
    "@auto-it/conventional-commits": "8.5.0",
    "auto": "^10.37.4",
    "eslint": "^8.17.0",
    "husky": "^8.0.1",
    "lint-staged": "^13.0.0",
    "prettier": "^2.6.2",
    "turbo": "^1.2.16"
  },
  "packageManager": "yarn@1.22.17",
  "auto": {
    "plugins": [
      [
        "npm",
        {
          "subPackageChangelogs": false,
          "monorepoChangelog": true
        }
      ],
      "conventional-commits"
    ]
  },
  "repository": "pchalupa/react-rtc",
  "author": "pchalupa <chalupa.petr93@gmail.com>"
}
