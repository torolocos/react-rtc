import { styled } from 'src/features/ui/theme';

export const Container = styled('div', {
  width: '40%',
  height: '50px',
  borderRadius: '3px',
  backgroundColor: '$body',
  alignItems: 'center',
  display: 'flex',
});
