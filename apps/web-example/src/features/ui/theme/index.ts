import { stitches } from './stitches.config';

const { css, styled, theme, globalCss, keyframes, getCssText } = stitches;

export * from './stitches.config';

export * from './globalStyles';

export * from './breakpoints';

export { css, styled, theme, globalCss, keyframes, getCssText };
