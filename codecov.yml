coverage:
  status:
    project:
      default:
        target: auto
        threshold: 5%
        base: auto
        branches:
          - main
        flags:
          - react-rtc
        paths:
          - packages/react-rtc
